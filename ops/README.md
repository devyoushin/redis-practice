# Redis Ops

Redis 운영 보조 자료를 두는 공간입니다.

| 폴더 | 내용 |
|------|------|
| `config/` | redis.conf, Bitnami Helm values 예시 |
| `install/` | Docker Compose와 systemd unit 예시 |

운영 개념과 절차 문서는 `docs/`에 두고, 실제 적용 가능한 설정 예시는 `ops/`에 둡니다.
